function cityRecord(name, population, treasury) {

    let infoCity = {
        name: name,
        population: population,
        treasury: treasury,
    }
return infoCity;
}
cityRecord('Tortuga',

    7000,

    15000);
